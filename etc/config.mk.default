# Bits build configuration.

# Each target must name a file like etc/target/*.mk
TARGETS:=

# Targets may require additional config, eg toolchain paths...

# Generic ops, replace with your preferred target.
run:;echo "not defined: make $@" ; exit 1
deploy:;echo "not defined: make $@" ; exit 1
edit:;echo "not defined: make $@" ; exit 1
